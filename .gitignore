# macOS
.DS_Store

# Sinh bởi XcodeGen (xcodegen generate); HandLive.xcworkspace thì commit
*.xcodeproj
xcuserdata/
DerivedData/
.build/

# Chỉ sinh ra khi chạy với HL_SWIFT_TESTING_PACKAGE=1 (kéo gói swift-testing); phụ thuộc môi trường, không commit.
Packages/*/Package.resolved

# Ký / secrets
*.p12
*.mobileprovision
.env
.env.*
